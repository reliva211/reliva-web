[build]
  command = "yarn cache clean --force && yarn install && yarn build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  YARN_VERSION = "1.22.22"
  NPM_FLAGS = "--prefer-offline --no-audit"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
